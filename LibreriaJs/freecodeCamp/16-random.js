/**
 * 
 * @returns 
 * 
Usa Math.random() para generar un decimal aleatorio.
Multiplica ese decimal aleatorio por 20.
Utiliza otra función,
 Math.floor() para redondear el número hacia abajo a su número entero más cercano.
 *
 Recuerda que Math.random() nunca devolverá un 1 y porque estamos redondeando hacia abajo
 *
 */


function randomFraction() {

    // Cambia solo el código debajo de esta línea
  
    return Math.random();
  
    // Cambia solo el código encima de esta línea
  }



  /***** ejemplo
   * Utiliza esta técnica para generar y devolver un número entero aleatorio entre 0 y 9.
   */

  function randomWholeNum() {

    // Cambia solo el código debajo de esta línea
  
    return Math.floor(Math.random() *10 );
  }